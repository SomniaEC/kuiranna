{% extends 'entidadBase/mostrarEntidad.html.twig' %}

{% block form %}

    <div class="row">{{ form_row(form.fechaRegistro, t_options) }}</div>
    <div class="row">{{ form_row(form.hechos, t_options) }}</div>
    <div class="row">{{ form_row(form.derechos, t_options) }}</div>
    <div class="row">{{ form_row(form.junta, t_options) }}</div>
	
{% endblock %}